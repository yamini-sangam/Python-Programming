# Given a list of names, concatenate them into a single string separated by spaces

# Sample list of names
names = ["Alice", "Bob", "Charlie", "David"]

# Concatenate the names into a single string separated by spaces
concatenated_names = " ".join(names)

# Display the result
print(f"The concatenated names are: {concatenated_names}")
